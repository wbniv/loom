| Date | Change |
|------|--------|
| [2026-08-25](https://github.com/wbniv/loom/commit/d2019ac) | spine-goal: §2.4's codomain filter at app-spine heads, opt-in |

<!--history-meta v1
d2019ac	author	Will Norris
d2019ac	added	305
d2019ac	deleted	0
d2019ac	files	1
d2019ac	body	`GoalTypePruner` filters a `ref`'s digest by the goal at that position and\nabstains wherever the checker synthesizes — including `app`'s function slot,\nwhich is where every held-out composition lives. `SpineGoalPruner` closes that\nhole at the only place it closes soundly: decode time, where the open `(app`\nparens have already fixed k, and the checker's synthesized per-position goals do\nthe instantiation a selection-time filter provably cannot (Amendment A1).\n\nThe proof: `check` does not special-case tag 4, `synth` tag 4 returns the head's\ncodomain after an unconditional `function_type[0] != 2`, and `synth` of a `ref`\nis the resolved type verbatim — so a k-ary spine synthesizes exactly the k-th\ncodomain, and acceptance is equality, `_instantiate` (only for a `forall`), or\n`_subsume` (whose precondition is erasure equality). A digest whose k-th\ncodomain is unquantified and erases differently cannot appear there.\n\nAbstains at k=0, wherever the spine has no goal, on `forall` heads and results,\nand on free tyvars. Effects turn out not to need an abstention at all: `synth`\ntag 4 returns the codomain whatever the call row is.\n\n§2.4's inline table is corrected rather than reproduced. Its `47 -> 7/13/13` is\nthe ∃k reading — recomputing it reproduces the plan's own named survivor lists\nbyte for byte, `list/reverse` and `clock/now` included, neither of which has a\ncodomain at the arity its spine actually uses. The position-exact rule that\nlanded gives 47 -> 6/4/5, and the route element survives in all three.\n\nEvidence (docs/plans/2026-08-25-mask-spine-refs.md §5, reproducible with\n`python3 -m experiment.spine_mask_probe`):\n  soundness  34 definitions x 3 tokenizations x layer on/off = 204 walks,\n             0 excluded continuations, 0 liveness fallbacks\n  precision  47 -> 6/4/5 exact (7/13/13 under the plan's ∃k reading)\n  overhead   mask_seconds_per_token 0.000752 -> 0.000811 (+7.8%), CPU, 1 stream\n\nThe soundness corpus gains the eight gold terms: `corpus/` reaches a binary\nspine at best, so the fixtures alone would have proved the layer sound over\npositions it never fires at.\n\n`PRUNER_NAMES` is unchanged and no shipped config names the new layer, so every\nrun on record is byte-identical; `KNOWN_PRUNER_NAMES` is what `build_pruners`\nand `runner.Config` validate against.\n\ntask prototype:test: 786 tests, OK (was 762).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01AqWeKNNAVguNfmda2TmpKY
-->
