| Date | Change |
|------|--------|
| [2026-08-26](https://github.com/wbniv/loom/commit/7ad7dda) | Hole machinery in prompts.py: obligations, closure, protocol and fill blocks |
| [2026-08-25](https://github.com/wbniv/loom/commit/2e1cacc) | Plan: hole-directed decomposition, pre-registered (next-lever §2.2) |

<!--history-meta v1
7ad7dda	author	Will Norris
7ad7dda	added	27
7ad7dda	deleted	1
7ad7dda	files	1
7ad7dda	body	Deliverable 3 of docs/plans/2026-08-25-hole-decomposition.md. The prompt-side\nhalf of §2.2's round: enumerate a draft's holes, close one back into a sub-task\ntype, ask for it, and splice the answer back where it came from.\n\n`hole_obligations(source, resolver)` walks the draft's term IR in pre-order and\nreturns each hole's path, its written goal type and its enclosing binder types.\nThe binder walk mirrors `scope.check_term`'s own depth bookkeeping, because that\nis the definition of which positions a de Bruijn index counts through. `lam`,\n`let` and `fix` write their binder types into the term, so a hole under them is\nfillable; a `match` arm that binds nothing adds nothing to the context and is\nfillable too; a hole under a `match` or `handle` binder is reported unfillable\nwith its reason, since that binder's type needs the scrutinee's synthesized type\n— `[mask-spine-refs]`'s machinery, not this plan's.\n\n`closed_subtask_type(declared_type_surface, obligation)` folds the context back\ninto `(fn t0 R0 (fn t1 R1 … T))`, with the effect rows peeled off the draft's\nown declared type in parallel with the top lambdas. A pure function of two type\nsurfaces, no term on either side, which is what makes the sub-goal derived\nrather than authored. `splice_fill` is the other half of the de Bruijn\nguarantee: it peels exactly the |G| lambdas whose annotations are the hole's\ncontext, refuses any fill whose prefix is not that, and drops the body at the\nhole's path — the innermost binder is index 0 in both places, and a closed\ndefinition has no free index left to misalign.\n\n`generation_protocol` joins `address_book` as a manipulated variable, pinned the\nsame way: `whole` is the default, and `whole` and `redraft` build byte-identical\nprompts to the ones this module built before holes existed — `redraft` differs\nfrom the control in the runner's loop, not its prompt. `holes` adds §3's block\nverbatim, in the address block's position, before any narrowing, so the\nprefix-identity property survives. Stripping the block gives back `whole`'s\nbytes for every task under every address book.\n\nBlindness is by signature: none of `hole_obligations`, `closed_subtask_type` or\n`build_fill_prompt` takes a `Task`, so none can read `composes` or\n`expected_surface`. The adversarial test of §4.8 check 2 pins it — two Tasks\nwith one spec, one declared type and different routes and gold produce\nbyte-identical prompts at every stage of every round, over two drafts and all\nthree address books.\n\nThe probe's `peel_arrows` / `closed_subtask_type` / `eta_skeleton` move here and\nare re-exported, so §1.3's round-trips and the protocol the runner will drive\nare the same machinery; the nested section now runs the real obligation-closure-\nsplice path and reproduces its pasted output unchanged. §8's deliverable 3 entry\nrecords the four choices §2.2 left open. Nothing runner-side: the loop, the\ntelemetry and the rollback are deliverable 4.\n\n33 tests added; suite 788 -> 821, green.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01AqWeKNNAVguNfmda2TmpKY
2e1cacc	author	Will Norris
2e1cacc	added	730
2e1cacc	deleted	0
2e1cacc	files	1
2e1cacc	body	The address-book run's §6 row 2 licensed decomposition as the next lever.\n§2.2's design problem — "whoever writes the sub-goals is doing part of the\ncomposition" — dissolves once the *model* writes them: it drafts `(hole T ())`\nwhere it is unsure (SPEC §2.6/§8.3), the checker types each hole from the draft\nalone, and the harness fills, splices and re-checks. Nothing proposing or\nvalidating a hole ever sees `composes` or a gold term; pinned by signature the\nway §4.2/§4.8 pinned the typed address filter.\n\nThree arms at matched completion-token purse: `whole` (control), `redraft`\n(feedback, no holes) and `holes` (= redraft + the hole protocol), so\nholes−redraft is the protocol and nothing else. Primary: per-cell\ncomposed-definition rate, one-sided Fisher at α=0.05, single comparison.\n\nThe probe lands the diagnostic and turns up two facts §2.2 did not have:\nacceptance (10% of cells) and type-exactness (37.5%) are each reached and\nalmost never together (2.5%) — the residual is a conjunction the protocol\ndecouples; and `score_semantic` scores a definition that is *nothing but a\nhole* as a mechanical-floor success, which §4.3.1 fixes before any arm runs.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01AqWeKNNAVguNfmda2TmpKY
-->
